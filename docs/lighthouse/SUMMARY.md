# Lighthouse score summary

Run `scripts/lighthouse.ps1` or `scripts/lighthouse.sh` with both apps running, then fill in scores from the generated JSON reports.

| Frontend | Performance | Accessibility | Best Practices | SEO |
|----------|-------------|---------------|----------------|-----|
| WordPress (localhost:8080) | _pending_ | _pending_ | _pending_ | _pending_ |
| Next.js (localhost:3000) | _pending_ | _pending_ | _pending_ | _pending_ |

Reports: see `*.report.html` in this folder after generation.
